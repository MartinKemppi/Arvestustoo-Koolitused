<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Koolitused.Views.EditTeacherPage"
             Title="EditTeacherPage">
    <VerticalStackLayout
        Padding="30,0"
        Spacing="25"
        VerticalOptions="Start">

        <Label
            Text="Õpetaja"
            FontSize="36"
            HorizontalOptions="CenterAndExpand" />

        <Entry x:Name="TeacherNameEntry" Placeholder="Nimi ja perkonnanimi"/>
        <Entry x:Name="RollEntry" Placeholder="Roll" Keyboard="Numeric"/>

        <VerticalStackLayout x:Name="RolesContainer" Spacing="10" />

        <Button
            Text="Salvesta"
            Clicked="OnSaveTeacherClicked"
            HorizontalOptions="CenterAndExpand"
            WidthRequest="200"/>
    </VerticalStackLayout>
</ContentPage>