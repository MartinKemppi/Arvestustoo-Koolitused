<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Koolitused.Views.DBListPage"
             Title="DBListPage">
    <!--<StackLayout>
        <ListView x:Name="koolitusListView" ItemsSource="{Binding}" ItemSelected="KoolitusListView_ItemSelected">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Padding="10" Orientation="Horizontal">
                            <Label Text="{Binding Koolitusnimi}" FontAttributes="Bold" />
                            <Label Text="{Binding Opetaja}" />
                            <Label Text="{Binding Kuupaev}" />
                            <Label Text="{Binding Hind}" />
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>-->
</ContentPage>